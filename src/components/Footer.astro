<footer class='footer-container' id="section4">
  <section class='top-footer'>
    <aside class='container-logito'>
      <img
        loading='lazy'
        src='./assets/footer/logito.svg'
        alt='logo'
        class='logito'
      />
    </aside>
    <h2 class='h2-title'>This is The thrill of riding <span>Ichiban</span></h2>
  </section>
  <div class='section bottom-footer'>
    <article class='bottom-top'>
      <img loading='lazy' class='left' src='./assets/banner.webp' alt='banner' />
      <nav class='bar'>
        <a href='javascript:void(0)'>🤖</a>
        <a href='javascript:void(0)'>🪼</a>
        <a href='javascript:void(0)'>😎</a>
        <a href='javascript:void(0)'>👻</a>
      </nav>
    </article>
    <article class='bottom-center'></article>
    <footer class='bottom-bottom'>
      <p class='left'>©2050 Ichiban Motorcycle Design. All rights reserved.</p>
      <aside class='right'>
        <h4>PRIVACY POLICY</h4>
        <h4>COOKIE POLICY</h4>
      </aside>
    </footer>
  </div>
</footer>

<script is:inline>
  const $logito = document.querySelector('.container-logito');
  const $h2Title = document.querySelector('.h2-title');

  document.addEventListener('DOMContentLoaded', () => {
    const observer = new IntersectionObserver(
      ([entry], obs) => {
        if (entry.isIntersecting) {
          $logito.classList.add('active');
          $h2Title.classList.add('active');
          obs.disconnect();
        }
      },
      { threshold: 0.8 }
    );

    observer.observe($logito);
    observer.observe($h2Title);
  });
</script>

<style>
  .footer-container {
    content-visibility: auto;
    height: 100vh;
    display: flex;
    flex-direction: column;
    background-color: var(--secondary-color);
    padding-inline: 18px;
    --footer-size: 250px;

    @media (width >= 4500px) {
      padding-inline: 8rem;
    }

    p,
    a,
    h4 {
      color: var(--primary-color);
      margin-top: 12px;
      text-wrap: balance;
      line-height: 1.4;
      font-size: var(--space-s);
      cursor: pointer;

      @media (width <= 1111px) {
        font-size: var(--space-xs);
      }

      @media (width <= 600px) and (orientation: landscape) {
        font-size: var(--space-2xs);
      }

      @media (width >= 3050px) {
        font-size: var(--space-l);
      }

      @media (width >= 4500px) {
        font-size: var(--space-xl);
      }
    }

    .top-footer {
      height: calc(100vh - var(--footer-size));

      @media (width <= 1111px) {
        --footer-size: 100px;
      }
      @media (width <= 500px) {
        --footer-size: 80px;
      }

      @media (width >= 3050px) {
        --footer-size: 400px;
      }

      @media (width >= 4500px) {
        --footer-size: 500px;
      }

      @media (width <= 600px) and (orientation: landscape) {
        height: 100vh;
      }

      &,
      .container-logito {
        display: flex;
        flex-direction: column;
        flex-wrap: wrap;
        place-content: center;
        gap: 1rem;

        @media (width <= 700px) and (orientation: landscape) {
          gap: 0rem;
        }
      }

      .container-logito {
        --size: 100px;
        width: var(--size);
        height: var(--size);
        border-radius: 50%;
        background-image: var(--navbar-gradient);
        margin-inline: auto;
        user-select: none;
        pointer-events: none;
        opacity: 0;
        transform: translateY(70px);
        transition:
          opacity 1s ease,
          transform 1s ease;

        &.active {
          opacity: 1;
          transform: translateY(0px);
        }

        @media (width <= 1001px) {
          --size: 60px;

          img {
            width: 50%;
            height: 50%;
          }
        }

        @media (width >= 4500px) {
          --size: 200px;

          img {
            width: 50%;
            height: 50%;
          }
        }
      }

      h2,
      span {
        color: var(--primary-color);
        margin-top: 12px;
        text-wrap: balance;
        line-height: 1.2;
        font-size: var(--space-l);
        font-family: var(--h2-font);
        text-transform: uppercase;
        width: 40%;

        @media (width <= 1111px) and (orientation: portrait) {
          font-size: var(--space-m);
          width: 60%;
        }
        @media (width <= 800px) and (orientation: landscape) {
          font-size: var(--space-s);
        }
        @media (width <= 400px) and (orientation: landscape) {
          font-size: var(--space-xs);
        }

        @media (width >= 3050px) {
          font-size: var(--space-xl);
          width: 30%;
        }
      }

      h2 {
        text-align: center;

        opacity: 0;
        transform: translateY(70px);
        transition:
          opacity 1s ease,
          transform 1s ease;

        &.active {
          opacity: 1;
          transform: translateY(0px);
        }
      }

      span {
        background-image: var(--navbar-gradient);
        background-clip: text;
        color: transparent;
      }
    }
  }

  .bottom-footer {
    display: flex;
    flex-direction: column;
    padding: 1rem;
    gap: 12px;
    height: var(--footer-size);

    @media (width <= 1001px) {
      gap: 8px;
    }

    @media (width <= 600px) and (orientation: landscape) {
      display: none;
    }

    .bottom-top,
    .bottom-bottom {
      display: flex;
      justify-content: space-between;
    }

    .bottom-top {
      .left {
        max-height: 50px;

        @media (width <= 1111px) {
          max-height: 25px;
        }
      }

      .bar {
        display: flex;
        gap: 29px;
        padding-right: 1rem;
        align-items: center;

        @media (width <= 700px) and (orientation: landscape) {
          display: none;
        }

        @media (width <= 1110px) and (orientation: portrait) {
          display: none;
        }

        @media (width >= 4500px) {
          gap: 100px;
        }

        a {
          text-decoration: none;
          color: var(--primary-color);
          text-transform: uppercase;
        }
      }
    }

    .bottom-center {
      min-height: 100px;
      display: flex;
      background-image: url('./assets/footer/dotsBottom.png');
      background-size: 300px;
      flex-grow: 1;

      @media (width <= 1011px) {
        min-height: 70px;
        transform: translateY(10px);
      }

      @media (width >= 3050px) {
        min-height: 170px;
        transform: translateY(10px);
      }
      @media (width >= 4500px) {
        min-height: 200px;
        transform: translateY(17px);
      }

      @media (width <= 600px) and (orientation: landscape) {
        display: none;
      }
    }

    .bottom-bottom {
      &,
      .right {
        display: flex;
        align-items: center;
        gap: 29px;
        color: var(--primary-color);
      }

      @media (width <= 1110px) and (orientation: portrait) {
        display: none;
      }
    }
  }
</style>
